# Synthetic live-model replay

Failed or incomplete run retained for review; not an acceptance PASS. Inputs and responses in objects/ are SYNTHETIC. model_calls contains immutable ObjectRef metadata and call records. Private config, credentials and runtime DB are excluded. The frozen inputs and independent oracle are in src/tests/fixtures/month. See report.json for exact checkpoint results.
