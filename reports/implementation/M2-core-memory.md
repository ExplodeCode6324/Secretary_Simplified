# M2 implementation evidence and remaining limits

Updated 2026-09-14. This is implementation evidence, not complete A23 acceptance.

Implemented: strict closed-schema Context with independent Manifest; actual input object evidence; atomic typed accept/commit; semantic request deduplication; model admission and recursive data-class checks; selected authority and dependency closure; real bounded READ_MEMORY with durable call budget and fresh Context hashes; transactional runtime controls; conversation summary CAS and durable retry cooldown; epoch-pinned memory slot controller with exact-slot verification and bounded retries; actual source/observation freshness; synthetic model input/output records including incomplete output; path-only validation feedback on semantic retries.

D07 machine/schema discrepancy and retrieval section were approved by Ayanami. Source documents: docs/contracts.schema.json, docs/DataStructure/Context.md, docs/DataStructure/ContextManifest.md, docs/MemoryPolicy.md and docs/Acceptance.md. Context field table has been synchronized. D05 additionally affects ExecutionProtocol, TypeRegistry, Verification and ConsciousnessState documents.

Verification performed: `go test ./...`, `go vet ./...`, and docs/checks/validate_docs.py passed. Separate tests cover exact wire schema/hash, dependency selection, nested disclosure, three-read budget, source freshness, summary CAS, typed failure not entering the model queue, fixed memory slot concurrency and epoch drift. These are deterministic integration tests, not real-model semantic acceptance.

The parent-controlled live month runs preserve failures independently. Run 3 was paused following provider HTTP 429. No complete month PASS is claimed here. The independent tools/livescenarios tool and reports/fixtures/live-scenarios/scenarios.json define seven additional frozen real-model scenarios. Offline fixture/SQLite/admission checks passed. Its first invocation stopped before any model call due to a relative SQLite path; that tool path issue was fixed. Live semantic execution is deferred until the parent confirms provider availability. The suite checks UUID-keyed item fields and unintended changes, records original utterances, independent oracles, every Context, outputs, Manifests, database and object evidence. Conversational agreement withdrawal is not WorldFact evidence withdrawal.

Remaining limitations at this checkpoint: memory.RefreshSlot validates the draft input before trimming potentially more than 100 recent changes; memory refresh does not yet persist the same external ContextManifest as core decisions; original candidate omission counts need tightening. Unified semantic failure records and pending-question resolution warrant independent re-review. Ayanami's original M2 review is not automatically closed by local tests; a follow-up was requested.

Follow-up after parent lifted memory.go freeze: RefreshSlot now trims to the schema's maximum 100 recent changes before validation, counts both loaded and snapshot-omitted candidates, preserves existing missing reasons, and then trims further for actual provider budget. Each attempt receives a fresh Context ID and a persisted standard ContextManifest with authority read set, exact final wire hash/bytes, output-schema hash, and per-section selected/omitted counts and JSON byte lengths. TestMemoryMoreThanHundredDeltasPersistsExactManifest passed with 125 actual item change events; TestTypedItemsAnd24HourMemory passed. These supersede the two memory-manifest/early-validation implementation gaps listed above, subject to independent review. A broad test invocation at this moment encountered two runtime acceptance failures (A08 boundary and A02 retry) in another agent's changing code; these were reported to the parent rather than mislabeled as a pass.
