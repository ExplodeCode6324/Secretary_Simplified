# A23 supplemental evidence and limits

Actual real-model execution used opencode-go gpt-5.6-luna on isolated SYNTHETIC databases. No typed command replaces an evaluated natural-language action. Original fixtures are in reports/fixtures/live-scenarios; each run preserved its exact fixture before any model request. Public response bodies, complete per-attempt Context, Manifest, original input, oracle and verified synthetic objects are available through the [public evidence index](../live-model/PUBLIC-EVIDENCE-INDEX.md). Runtime databases, configuration, tokens and grants are not included; logical audit records support database-free reproduction.

- [Original failed scenario run2](../live-model/scenarios-run2-failed/report.json): 6/7 passed; prior-session retrieval failed because all three matched records were discarded by budget and the model exhausted retrieval attempts. The oracle was unchanged.
- live-scenarios-run3: same seven original oracles passed after preserving a historical anchor and removing unrelated fallback items.
- live-retrieval-regression-run1: original agreement/withdrawal/retrieval inputs and oracles reused unchanged; 3/3 passed after excluding the current query turn from historical evidence. Final Context contained both actual Master statements: old BLUE731 and the later withdrawal/new GREEN842. The actual model reply was GREEN842. This establishes retention of this specific corrected agreement across sessions, not general memory reliability.
- [Final scenario run4](../live-model/scenarios-run4/report.json): all seven oracles passed after D09 explicit RetrievalServed/zero-hit envelope, self-exclusion and net-ref omission corrections. The exact-ID case separately verified IDs ending 701 and 702 despite identical titles, unchanged 701 priority/revision, and 702 final revision3/priority3 after a real response was made stale by an injected concurrent revision. Dependency completion and unlock preserved the exact dependency ID.
- live-world-read-run1: 2/2 world-state reading cases passed.
- [World reading run2](../live-model/world-read-run2/report.json): strengthened fixed conflict membership oracle also passed 2/2. Two frozen explicit assertions were committed through actual grant/permit/WorldCommitService code and produced one conflict group containing fixed fact IDs801/802. The model replied UNRESOLVED_CONFLICT. After a frozen explicit retraction with real evidence and permit, fixed802 was RETRACTED and fixed801 remained CONTESTED; the model replied COFFEE_WITHDRAWN_UNRESOLVED. This checks reading conflicts/retraction, not model-generated world mutation authorization.

D09 code was authorized as a defect correction. Ayanami's DeepSeek design response conditionally agreed; E1-E6 were implemented in core/core.go, context/context.go, store/retrieval.go and tests/retrieval_budget_test.go. MemoryPolicy was updated only after that response. Independent final program review is still separate. Mechanical tests cover zero-hit versus not-served state, E/N multi-record eviction, latest no-Evidence fallback anchor, self-exclusion, duplicate ref coverage, long-record omission, required dependency overflow and durable READ_MEMORY. These are not replacements for real-model cases.

No-retrieval source impact: the default branch still permits the original fallback selection, has no retrieval envelope and keeps the original section reason string. A controlled test compiled the old context implementation from git2b753f2 alongside the corrected builder. Same snapshot/time, with only the independently generated context_id normalized to one valid UUID, yielded exactly equal encoded request bytes for both fallback and explicit-ID cases (27769 and27837 bytes). Evidence is in retrieval-wire-parity/report.json with the original test source. Later D09 changes preserve the same default RetrievalServed=false branch; this is bounded branch evidence, not a claim that every later model/schema revision has identical bytes.

The [month run4 report](../live-model/month-run4/report.json) finished 90/90 item checkpoints and 30 generated consciousness snapshots. Its original report reused a mutable item_ids map, so earlier entries contain later IDs. That original report remains unchanged. scripts/audit_month_evidence.py independently reconstructs every checkpoint from immutable Item events before the InputTurn's physical updated_at. It verifies strictly increasing boundaries, exactly one Item event per checkpoint, and the historical title/ID/state/date/null set against the authored oracle. [report-identity-correction.json](../live-model/month-run4/report-identity-correction.json) records original report hash, logical event hash, per-checkpoint inclusion/exclusion boundaries and reconstructed IDs: 90/90 passed. It never clips a final ID map using the oracle.

The same read-only audit checks all30 snapshots' EntityReadRefs against the exact Item revision at snapshot_seq: zero mismatches. Beginning/middle/end samples (days0,14,29) preserve full original inputs, snapshots and historical state in [consciousness-free-text-samples.json](../live-model/month-run4/consciousness-free-text-samples.json). Day0's three OPEN items/date claims, day14's03-05 completion and06 earliest open item, and day29's all20 cancelled plus08-10 removed due dates are grounded. Day0's wording “highest priority” is ambiguous: actual priority1 ties for highest in that current set, but the absolute scale defines0 as highest. We do not certify all free-text semantics as perfect.

A23 coverage assessment: the combined fixed month and supplemental sets cover four domains with five items each, creation/completion/rescheduling/cancellation/unknown time, exact persistent IDs, optimistic conflict, explicit WorldFact conflict membership and retraction reading, source staleness, dependency unlock, and prior-session corrected agreement. Scope limits remain: supplemental cases are separate isolated sequences rather than one integrated30-day world history; world mutation itself is frozen setup rather than model-generated authorization; only sampled consciousness language is checked. A23's strict key-field evidence can be assessed from this combined set, but independent review must decide whether it satisfies the intended integrated acceptance scope. Do not rename this combined evidence as complete M1-M3 acceptance or REAL_USE.

Publication verification: [public-evidence-reproduction.json](public-evidence-reproduction.json) confirms exact byte reproduction of the 90-point identity correction and 30-snapshot reference audit using only public [audit-records.json](../live-model/month-run4/audit-records.json), the original report and frozen oracle. The [reference audit](../live-model/month-run4/consciousness-reference-audit.json) and all object hashes are independently inspectable. Intermediate run3 and directed-regression results above are historical process notes; the public acceptance comparison uses the preserved original failure and final run4, without replacing the failed source. No README was changed during this evidence publication.

Latest archived checkpoint: [month run5](../live-model/month-run5/report.json) completed90/90 Item checkpoints and30 consciousness snapshots with no reported failure. Independent read-only [identity audit](../live-model/month-run5/report-identity-correction.json) passed90/90 and matches every original per-checkpoint ID map (the tool alias bug is absent); [reference audit](../live-model/month-run5/consciousness-reference-audit.json) passed30/30. [Days0/14/29 language samples](../live-model/month-run5/consciousness-free-text-samples.json) support the three OPEN initial work items,01-05 completion and06 due September29 06:00, then all20 cancelled and item10 date withdrawal. Qualitative priority descriptions remain separate from exact numeric comparison. Public-only audit reproduction is byte-identical. This run is retained at its actual source/prompt time; it does not establish equivalence to any later default-reminder or provider-role-instruction change.
