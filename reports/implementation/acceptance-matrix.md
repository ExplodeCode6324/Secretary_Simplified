# A01–A25 implementation evidence audit

Audit snapshot: 2026-09-14, final2 frozen build. This matrix maps evidence to `docs/Acceptance.md`; it does not replace the specification or turn deterministic tests into live-data acceptance.

**Issue #1 follow-up complete:** AUD-01 through AUD-04 repairs, GATE-01/02 and the related bounded diagnostic persistence fix passed targeted regression and actual Ayanami DeepSeek review. [New release regression](issue1-regression.json) records whole-repository race/vet, final test-only additions, build hashes and a passing short CLI/Core/Runner smoke. The supported real-data entry is [PERSONAL text in independent directories](../../docs/RealDataTrial.md); real file-source synchronization is not open. Under Master's explicit acceptance criteria the original final2 soak continues unchanged **without restarting or repeating A25**. Its eventual duration result applies to the old binary hashes below, not the new audit-fix release. This is synthetic evidence, not completed real-data testing.

**Pre-audit baseline status (retained with original build identity):** D09/D11 question retrieval and D12 output-classification defects are closed by the evidence below. Final2 full race, vet, DOC_ONLY and builds pass. The public real-model `question-run4` chain passes. `month-run8` finished all 90 Item checkpoints with zero failures and 30 consciousness outputs; its independent identity/summary audits and public evidence archive are complete (A23 ledger v2: 129/129). The original final2 two-hour run is still in progress; audit fixes do not require a new duration test. Old 7,200-second results cannot substitute for it. Implementation and reviewed evidence were pushed to public main at 2ba8547 (local/remote divergence 0/0); the final soak result remains to be recorded; this snapshot does not claim all A01–A25 complete.

Final2 binary identity: secretary `e60c16167cc42cff76f231adfbf31c44711932389e7262c7b2dd1d843d365c17`; secretaryd `6682c7395f1d9ca6a7aa840f7d6f9798c96f0257a698bd7c30e5632632d12950`.

## Current evidence anchors

- [Final2 offline checks](final2-offline.json): full `go test -race ./...`, `go vet ./...`, DOC_ONLY validation and both binary builds; exact source and binary hashes.
- [D12 Core/Memory independent DeepSeek verdict](../../review/D12-core-memory-deepseek-final.response.md) and [D12 runtime/World verdict](../../review/D12-runtime-world-deepseek-final.response.md): PASS in their defined scopes, no remaining must-fix. Independent probes supplement implementation tests.
- [Final Schema/prompt equivalence review](../../review/final-schema-prompt-deepseek.response.md): final shared-extension Schema and compact prompt checked after the D12 reviews; byte-metric serialization clarification is documented separately.
- [Public question-run4 report](../live-model/question-run4/report.json): actual CLI/Core, real model, program-created question, cross-session READ_MEMORY, original-session explicit answer after restart, stable question identity and resolved-target refusal; final2 binary hashes match.
- [Public month-run8 report](../live-model/month-run8/report.json): 90/90, zero failures, 30 persisted consciousness outputs; exact identity and reference audits pass. [A23 ledger v2](A23-coverage-ledger-v2.md) records the complete month8 + scenarios4 + world2 selection (129/129), reproducible public evidence and preserved failure history.
- Current machine tests retain their individual oracle and fault scopes below. D12 propagation, strict ingress/defaults, D09 dual anchors, D11 lifecycle and legacy non-mutation are included in the final2 full race check.

## Matrix

| ID | Evidence status | Existing evidence | Scope boundary / remaining evidence |
|---|---|---|---|
| A01 | PASS for strict public ingress | TestAcceptanceA01PublicMalformedBodiesHaveNoDurableWrites snapshots every application table, including events/receipts, before and after nested unknown fields, invalid enum/schedule, duplicate JSON key and a later invalid action after a valid action through public HTTP handlers; all rejected with zero durable changes. Race passed. Existing tests cover oversize bodies; DeepSeek also executed actual UDS ingress probes. | Mechanical ingress evidence; model semantic correctness remains separate. |
| A02 | PASS for typed persistence fixture | `TestAcceptanceA02TenRetriesAcrossReopen`: identical serialized actions replayed ten times, closing/reopening SQLite each time; exactly one InputTurn, Item, Task, ScheduledJob and JobRun; different payload same key denied. | This is application/store restart, not OS process crash injection or live-model semantics. |
| A03 | PASS for concurrent CAS fixture | `TestAcceptanceA03IndependentConnectionsCAS`: two independently opened Stores contend concurrently on revision 1; exactly one succeeds, final revision 2, exactly two events including creation. Existing test checks current revision in conflict response. | Public HTTP mapping remains covered separately as final routes are completed. |
| A04 | PASS for SQLite write-boundary fixtures | `TestAcceptanceA04AtomicCommitBoundaries`: independent persistent SQLite triggers fail before state, event, and receipt insert; an explicit failure after receipt before commit also rolls back all three tables. Each retry commits exactly one state/event/receipt. | These are real transaction rollback tests; they do not simulate OS/power loss during SQLite commit. Reference/cycle rejection is covered by foundation tests. |
| A05 | PASS for fixture ingest scope | Duplicate/version conflict/tombstone tests plus `TestAcceptanceA05EmptyPageAndFailurePreserve`: empty success increments revision and preserves non-null freshness; cancelled page delivery leaves exact previous SourceState, one SourceRecord and one ObjectRef. | Failure uses cancelled input delivery, not a live upstream outage; real connectors remain M4. |
| A06 | PASS for declared world-permit mechanical matrix | `TestAcceptanceA06WorldPermitDenialMatrix`: valid dedicated task commits; missing permit, wrong entity/predicate/operation scope, grant revoked after dispatch, expired grant/permit, old fence and forged proposal value reject. Rejection preserves world/event state and does not consume the permit. Existing exact-run and consumed-permit tests complement it. | World/D12 independent final review now PASS. Do not expand this world scope into unrelated source/path capability combinations or require unspecified exhaustive principal permutations. |
| A07 | PASS for history, conflict projection and stale-derived-authority mechanics; live reading scoped | ASSERT/CORRECT/RETRACT history tests; `TestAcceptanceA07RetractionInvalidatesDerivedWorldSnapshot` generates a prior summary through Memory with a deterministic model fixture, then real retraction makes the old WorldFact reference stale: current Context retains RETRACTED revision2 and omits the derived snapshot while preserving history. Public world-read-run2 additionally verifies exact conflict membership and live-model withdrawal reading. | This does not certify every newly generated free-text summary. A23 records sampled language limits; no requirement for unlimited adversarial prose cases is inferred. |
| A08 | PASS for specified slot and intraday mechanics | Exact 23:59/24:00 boundary and SQLite close/reopen tests; concurrent same-slot registration; missed3-slot/current-only and epoch pin tests; `TestAcceptanceA08IntradayAuthorityOverridesEarlierSnapshot` verifies current Item revision2/DONE enters Context within the same slot and stale derived content is omitted without regenerating consciousness. | Restart evidence here is actual Store close/reopen. OS-process-kill injection is not separately specified by A08 and must not silently become an additional completion gate; process evidence belongs to its own scoped tests. |
| A09 | PASS for the agreed explicit-ID question and retrieval scope | Existing summary CAS/watermark/restart tests plus D11 normal Core creation, competing answers and atomicity tests. Public `reports/live-model/question-run4/report.json` passes the full real-model CLI chain: generate question, cross-session READ_MEMORY recovers original ASSISTANT question block/session/ID, restart and answer in original session, stable replay and 409 already-resolved refusal. Final2 binary hashes match. | The approved scope uses explicit question IDs in the original session; arbitrary pronoun resolution or answering in any session is not claimed or required by D11. Earlier seeded-metadata tests alone were insufficient; the live normal path now supplies that evidence. |
| A10 | PASS for current bounded wire, disclosure and omission mechanics | Final2 full race includes exact Encode/wire/Manifest hashes, 125-delta clipping, necessary dependencies, D09 E/N dual anchors, zero-hit envelope, self exclusion and net-reference omission. Shared RegisteredExtensions expands to the identical original Schema; compact prompt and final Schema have separate independent review. | Historical oversized attempts remain preserved. The 32,000-byte limit and oracle were unchanged; actual final2 question and month runs supply final-prompt execution evidence. |
| A11 | PASS for fixed recurrence/misfire/overlap fixture oracles | Calendar hand-authored UTC oracles plus `TestA11FixedMisfireAndGraceOracles` (10 cases) and `TestA11OverlapSkipsEveryUnresolvedState` (4 states) cover zero/exact/outside grace, SKIP, interval/daily/weekly catch-up aggregation, omitted counts, exact next_due and repeated-scan dedup. Existing event/restart and DST/fold/downtime audit tests also pass. | Synthetic deterministic expectations are independent of NextOccurrence output; no real model or runtime semantic change. Evidence: `A11-misfire-overlap-checks.json`. |
| A12 | PASS for deterministic cancellation boundaries | `TestAcceptanceA12CancellationFourStages` exercises pre-claim, claimed-before-permit, dispatch-before-effect and effect-before-receipt; no new notification after cancellation, truthful late effect receipt, Task stays CANCELLED. Dispatch and permit creation are atomic in this implementation. Active Core context cancellation and artifact/expired-permit probes also pass. | Scope is mechanical local/control safety; arbitrary external systems are not included. There is no separately observable permit-created-before-dispatch transaction state. |
| A13 | PASS for actual worker-process exit and local reconciliation | `TestAcceptanceA13ProcessExitsAfterEffectBeforeReceipt` launches an independent test worker process, commits notification then exits without receipt or clean Store close. Parent reopens DB, lease recovery yields UNKNOWN, query reconciles without dispatch, effect count remains 1 and attempt remains 1. Remote UDS proof identity/rebinding tests supplement this. | Tests abrupt process exit after a persisted local effect, not power failure or an unimplemented external non-idempotent connector. |
| A14 | PASS for persisted fence mechanics; world review separately scoped | `TestRuntimeDurableFenceAndUnknown` advances takeover fence and rejects old dispatch, preserves UNKNOWN after dispatched lease loss; `TestStaleWorkerCannotCommitReceipt` rejects stale Core completion, UDS query rebinds old evidence into a new receipt and rejects changed commands. | World-specific authority review is listed under A06; these do not authorize an old worker or prove an arbitrary connector’s behavior. |
| A15 | PASS for six durable WAIT fixtures | `TestAcceptanceA15SixWaitPaths`: already satisfied, simultaneous registration/write through separate Store connections, lost callback, DB reopen, old generation cancellation, timeout. Each leaves exactly one WOKEN/EXPIRED generation and second scan returns zero continuations. | Scope is SQLite predicate/controller behavior; no OS process kill was injected. |
| A16 | PASS for fixed runtime criterion and independent evidence mechanics | Original tamper probes, canonical criterion hash, REPLAN immutable criteria and D08 three-kind current-run/attempt/fence proof tests pass; altered expected/extra keys, model briefing actions, empty/tampered objects, old run/fence/source proof fail. Precise consciousness-slot verification is covered separately. | PASS means the declared machine-checkable condition only, not briefing quality, awakening or business Item completion. Independent implementation review is closed by the current runtime/World verdicts. |
| A17 | PASS for the agreed classified-data and source-authority scope | Existing SOURCE authority/SECRET canary tests plus D12 synthetic canary propagation through ASSISTANT/questions, Item create/update, summary/consciousness, runtime/World and artifacts; SYN-only rejection, authorized PERSONAL positive controls, nested injection rejection and legacy OUTPUT_CLASS_UNKNOWN with zero rewrites. Both independent D12 DeepSeek reports PASS; final2 full race PASS. | No real secret used as test payload. Classified input remains in authorized durable storage; model/log disclosure is separately guarded. This is the finite D12 information-flow contract, not a guarantee about all possible natural-language wording. |
| A18 | PASS for persistent program budgets and retry ceilings | `TestAcceptanceA18AllBudgetFieldsSurviveReopen` exhausts all six budget dimensions and proves each still rejects after DB reopen while a new root succeeds. Event cooldown, independent recurring roots, retrieval boundedness and no-effect/effect-observed retry tests also pass. | This proves deterministic program enforcement; live-model attempts are separately traced in A23. |
| A19 | PASS for the only supported baseline version | Actual new-file baseline 0→1, injected DDL rollback leaving zero tables, incomplete initialization refusal, unsupported version/checksum refusal, and SQL/JSON mirror checks pass. | No prior released business schema exists to upgrade. This is not a promise of an arbitrary future upgrade pipeline; any later schema version needs its own migration and rollback tests. |
| A20 | PASS for implemented backup/restore fixture | Concurrent writer backup, object/hash tamper refusal, corruption refusal and manifest requirements pass in current source tests. Independent review also started restored processes and verified default frozen state, queued attempt zero and no CoreWork. | Source hashes and review scope must remain attached; real user dataset restore is deferred until data is provided. |
| A21 | PASS for scoped public fixture chain | D08 `TestD08PublicAdmissionExecutionAndNegativeEvidence` now registers alarm through Core.Typed with program-derived alarm_session_recorded, then Runner and independent Verifier pass; snooze old session denied/new session accepted/STOPPED accepted. Separate release-process offline control test proves persisted muted settings, repeat snooze dedup, stop and health without Core. | Public admission test uses current source in-process; existing real-process fixture uses pre-D08 binary with Store registration. Final2 offline/public CLI checks and independent runtime review supersede that earlier pending review. Real audio/awakening is excluded. |
| A22 | PASS for actual minimal fixture CLI/process chain | Existing actual CLI init→typed input→scheduled execution→notification receipt trace passes and Item remains OPEN. Current typed HTTP/CLI route and D08 public admission fixtures expand this. | Natural-language/live-model quality is A23, not an extra requirement silently added to this minimal deterministic chain; final2 question-run4 now supplies live CLI evidence in its separate agreed scope. |
| A23 | PASS in the fixed synthetic real-model scope | Public month-run8 contains 90 passing Item checkpoints and 30 persisted consciousness outputs, with passing exact-ID/reference audits. A23 ledger v2 selects complete month8 + scenarios4 + world2 datasets: 129/129; final2 question-run4 separately closes the normal question lifecycle. | Synthetic data and sampled semantics only. Ledger v2 is a retrospective evidence index, not a claimed pre-call combined freeze. All earlier failed runs and ledger v1 are preserved. |
| A24 | PASS, final2 DOC_ONLY | `final2-offline.json` records the final shared-Schema/compact-prompt DOC checker PASS; primary contracts 40, shared definitions 58, baseline DDL unchanged. The final Schema equivalence review independently verifies full normalized structure equality. | DOC_ONLY covers contracts/DDL/examples/links, not model or real-data behavior. |
| A25 | RUNNING: final2 two-hour soak | Current duration run uses secretary e60c1616… and secretaryd 6682c739… from final2. The earlier archived release-soak report has 7,200.010 seconds/240 samples/zero failures for a different binary and remains historical evidence only. | Await the final2 run's own full 7,200 seconds and result bound to these hashes. Do not add durations across builds or substitute virtual 72-hour replay. This is the sole still-running duration check at this snapshot. |

## Remaining closure and permitted claims

The supported claim is “final2 implementation and offline checks pass; A09/D12 reviewed defects are closed; final2 live question/month execution passed.” The final2 two-hour soak is still running, and month-run8 read-only audit/public packaging is being completed. The coordinator will append the actual soak result and publication status without substituting older build evidence.

M4 real source data, real audio and 30 actual calendar days (A26 onward) remain outside this delivery claim. Master will provide real data after deployment. Keys, private configuration, runtime databases and unredacted private traces must remain outside public evidence.

## Historical investigation and superseded status notes

Everything below records earlier checkpoints and preserved failure history. Earlier BLOCKED/Partial/pending-review/running statements are historical, **not current overrides**; current rows and evidence anchors above control. Original failure reports and original model outputs are retained unchanged. The D11 retrieval/overflow failures and D12 synthetic downgrade probe were necessary evidence that led to fixes, not current unresolved findings.

## Mechanical gap follow-up

Added `src/tests/runtime_acceptance_test.go` and `src/store/runtime_acceptance_test.go`; `go test -race ./tests ./store -run TestAcceptance -count=1` passed. These upgrade only the explicitly described scopes above. Full final-repository regression remains necessary after concurrent HTTP/CLI changes. The job PATCH increment also passed `go test -race ./tests ./store`; Ayanami review of that new increment remains pending; the earlier provider quota interruption was subsequently lifted. No new live-model call was attempted for this audit.

## Later final-source checkpoint (2026-09-14 04:18 +08:00)

Historical update (superseded where noted below): Final offline evidence is now final-offline.json. Whole-source race/vet, final DOC validator, two-process integration and release-binary smoke passed again. The prior A24 stale-report observation is superseded by the new DOC_ONLY PASS. API/CLI additions and exact typed retry response checks passed locally; independent review remains BLOCKED_NO_REVIEW_CONCLUSION. A23 run3 stopped on GoUsageLimitError after 26 passing checkpoints. Final-build two-hour soak has restarted and adds repeated real CLI writes with durable-count and queue checks; earlier build runtimes are not accumulated. No full A01-A25 or real-use approval is claimed.

## Release-build follow-up, 2026-09-14

The earlier RUNNING soak and stale document observations are superseded by the A24/A25 rows above. Full soak report and SHA256 verification are archived under `reports/implementation/release-soak/`. Only test/report files were changed in this follow-up. A04/A05/A21 tests use synthetic data and no model API. A21 deliberately reports Store registration separately from the currently missing public Core alarm admission path; D08 review is coordinated by root. Current live month replay and independent reviews continue separately.

## D08 and D06 implementation follow-up

D08 now adds three fixed criteria with unchanged immutable hashes: alarm session bound to unique task run; briefing final successful current attempt/fence receipt with nonempty verified object bytes; source current execution provenance in append-only source.synced events. Public Core.Typed fixtures cover all three and reject altered criteria; corrupt/empty artifacts, stale receipts, old source runs and null cursor cannot pass. A stub returning briefing actions is rejected without applying those actions. `runtime.source_sync` has exactly four program-owned validated fields. This does not establish briefing content accuracy, real audio or awakening.

DeepSeek D01–D07 revalidation identified a DST downtime audit omission: calendar catch-up used only the final latest→next interval. The correction scans original due→next and `TestRuntimeDSTGapDuringDowntimeAuditedOnce` proves a multi-day catch-up records the gap exactly once. Design semantics are unchanged.

### Open D08 amendment: REPLAN current-run identity

Historical issue, now corrected by the amended DeepSeek decision: the initially approved `exactly one run per Task` predicate conflicted with legal REPLAN, which retains the older run. The red test initially reproduced the failure. Its temporary opt-in gate has now been removed: it runs normally and passes. Current run uses latest admission rowid with older nonterminal-run rejection; three-kind lineage tests prove two REPLAN generations, database reopen, old evidence exclusion, new proof success, delete-old-proof independence, delete-new-proof denial, verify-before-REPLAN terminal refusal, and wrong source fence denial. Independent implementation review remains pending.

D08 amendment also retains alarm evidence at run granularity and briefing/source proof at current attempt/fence granularity. Runner no longer overwrites a remote FAILED receipt’s effect_observed field: a no-effect failure follows persistent retry ceilings (three attempts), while an effect-observed failure is not blindly repeated. Both paths have explicit tests.

Final follow-up verification: `go test -race ./...`, `go vet ./...`, and the full DOC_ONLY validator passed after D08-R1/R2, the D06 downtime-gap correction and remote-effect preservation. Source/test hashes are recorded in `reports/implementation/D08-runtime-checks.json`. This is self-test evidence; independent Ayanami implementation review remains pending.

## Runtime evidence closure distinction

The original broad Partial labels for A12/A13/A14/A16/A18/A19/A20/A22 were narrowed to the implemented, explicitly required mechanism and updated with fresh evidence above. A13 now has an actual child process exit at the persisted-effect/no-receipt boundary, not merely an in-memory simulation. A12 and A18 have explicit stage/budget matrices. For A19, demanding an upgrade from a nonexistent previously released business schema was an overbroad audit condition; the supported baseline/version refusal and rollback are tested. The previously remaining A11 misfire/overlap state-count gap is now closed by 14 explicit cases; the next release binary’s own soak is separately ongoing. Model semantic coverage and independent review remain separate completion gates.

A11 closure: fixed oracle tests passed with the production source frozen. `SKIP/grace_seconds=0` at due+904ms is explicitly SKIPPED; prompt/default-policy corrections must not rewrite this runtime meaning. The first test setup used a future date beyond its short synthetic grant and was corrected to a fixed past Monday before rerunning successfully; no production authorization change was made.

## M1/M2 mechanical follow-up (current implementation)

Added only `src/tests/m2_acceptance_security_test.go` and `src/tests/m2_acceptance_continuity_test.go`; no production change in this follow-up. Race command and scope are recorded in `m1-m2-mechanical-followup.json`. A06/A07/A08/A10/A17 rows now distinguish their actual passing mechanical checks from independent review and future-source binding. A09 remains partial for a concrete normal-path question-creation/resolution requirement; manually seeded metadata is not passed off as that workflow. Older rows demanding every possible scope permutation, all gap permutations, or a separately injected OS crash where the criterion says restart were too broad and are superseded. A01 is handled independently by the parent; its new public schema atomicity test was not edited.
