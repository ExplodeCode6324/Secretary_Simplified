同session仅短收口，无工具：固定Ctrl+S发送且raw关闭IXON，另支持固定Alt+Enter备用，Enter换行，PTY证明真实可用，帮助说明；是否同意不新增发送键配置项/配置系统（Issue要求通用按键，没要求可配置）？metadata可每客户端独立文件+实例绑定避免共写（若共享同文件才flock），同样满足。非TTY chat从pipe逐行到EOF可以阻塞等待输入但不键盘接管；--json必须纯JSON不能“状态行+JSON”，原output本就JSON，保持。新增history用独立conversation sequence，不混业务events seq；questions/pending都在authority snapshot聚合，无需强制再建独立路由。粘贴中CtrlS不能触发发送：bracketed paste消息按整段草稿插入并过滤控制键，未bracketed输入无法识别粘贴边界须明确限制，不能把ESC/控制内容升级操作。请同意这些最小实现收敛或具体反对。无旧测试/长测门槛。
