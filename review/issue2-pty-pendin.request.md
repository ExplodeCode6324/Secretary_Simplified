同TUI设计session，快速只读判据核查，不重开全审：runtime实际macOS PTY首2轮3项PASS，但退出termios逐bit比较FAIL，before lflag1483 after536872395，唯一差PENDIN 0x20000000，ICANON/ECHO/ISIG、iflag/oflag/cflag/speed/cc均还原。原报告issue2-tui-pty-run1/2保留。拟校正PTY判据：验证用户配置模式全部恢复，仅忽略内核pending-input瞬态PENDIN，不改生产、不抹历史，清楚报告probe校正。请以本机头文件/官方termios有限资料核实PENDIN语义，确认是否允许此精确排除（不能泛屏蔽差异），若必要指出生产恢复。只新TUI12终端恢复范围，不旧测试/长测。另x/ansi v0.10.1现有传递依赖提升direct仅Unicode cell测量，原pin不变，是否允许作为此前TUI依赖例外的最小补充，文档已有同步，不另重审供应链。最多4工具轮，留自然final，stdout归档。
