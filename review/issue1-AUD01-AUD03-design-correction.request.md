同一设计商议，请仅基于已读事实和此更正，禁止新增工具/扫描，给最终可实施短裁决。你的初稿称本回复无网络模型调用不准确：实际 Hermes reviewer 使用 opencode-go DeepSeek，此处只能说未另调被测 Secretary 模型。
关键纠正：database/sql 会在 ctx 取消后自动 rollback 释放 SQLite writer，故 R-a defer 不保证仍持 writer。Foundation改为固定 objects/.publish.lock flock 跨进程锁（永不删除锁 inode，ctx 可取消等待）；所有 PutObject/Accept/恢复统一 flock→SQLite IMMEDIATE，直到 commit结果确认/清理才释放flock。取消/commit错时仍持 flock，以 bounded uncancelled persistence ctx 查 object_ref：存在保留+校验，确定无row才删自己新建路径，查询不确定保留完整孤儿。不允许任何业务/model继续脱离取消运行。
API Store.WriteObjects(ctx,func(tx,*ObjectWriter)error)，ObjectWriter.Put只有在已持flock+tx时可用（不公开裸PutObjectTx绕锁）。受理先tx幂等/answer/queue，再完整临时写+sync+close→hardlink no-replace→dirsync→ref和业务同tx；允许临时写在flock/tx内以避免你的锁外清扫活文件竞态和增加协议，已有大小上限；SQLite锁5s带来的等待可诊断错误不应强制引入新锁外暂存方案，此处请区分性能建议和安全硬条件。恢复同flock仅清已知temp，不碰committed；hardlink失败failclosed绝不copyrename覆盖，无需强制每次启动探测才安全。坏无ref final隔离；committed损坏保留拒绝；objectMu移除，tx读取用tx，semantic hash兼容。
请确认该最小方案是否同意，明确替代初稿R2/R3/R-a的内容。不增加purge CLI/新配额/时长门槛；隔离可由现有文件级运维枚举，非必需新命令。runtime briefing也可此API将ObjectRef和FinishWorkTx原子绑定。剩余真正 must-fix 仅列具体安全理由。原首稿保留历史，不改其内容。
