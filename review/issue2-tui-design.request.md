Master新增Issue2，只验AUTH01-08/TUI01-13/DOC01-05/BUILD01，不重开旧issue1或旧context/storage/全仓/月回放/长期测试。请实际Ayanami DeepSeek读review/issue2-original.md新TUI与唯一会话边界，商议此最小实现方案，最多有限相关CLI/Core查询代码查证，不扫描旧审查。
Runtime提案：Bubble Tea v1.3.10+Bubbles v0.21.0官方稳定v1 tags锁go.mod（是否允许现有直依赖规则最小例外）；textarea+viewport用于中文/括号粘贴/resize，async Cmd有界HTTP+poll退避，单UI goroutine无model/DB/shell子进程。foundation将提供后端authority/history接口与GET requestID恢复。发送前0600元数据仅instanceID/requestID，正文不持久；失响应只查原request，不换ID重发；活进程可保留原信封手动retry同ID，重开若404显示未确认需用户处理，不静默丢/重投。外部文本删除ESC/OSC/CSI/C0/C1（保留换行tab）后渲染；secret/token不日志。Enter换行Ctrl+S发送（paste不自动提交）；Tab面板PgUp/Dn滚动Ctrl+P/N内存输入历史；Ctrl+C/quit只退出。结构问题选择绑定ID；Task取消/Job暂停恢复/通知ack按选中target/revision确认调用既有RunnerAPI，Coreoffline不阻塞Runner。macOS Python pty+TIOCSWINSZ实际中文3轮/paste/resize/延迟编辑/重连/终端恢复与非TTY无ANSI。只指定本轮测试race。
请明确同意或必要修正：唯一会话非多会话、metadata实例隔离/权限、TUI非TTY --json/plain兼容、request ambiguity恢复、安全控制序列、Ctrl+S终端IXON兼容/通用可用发送键、依赖范围。不要增加远程/真实付费模型/旧回归门槛，代码未实现本轮仅设计裁决。stdout存报告，留自然final，最多8工具轮。
