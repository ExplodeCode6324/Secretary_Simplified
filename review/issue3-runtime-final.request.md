Master授权Issue3 RES01/02实现独立终审，范围仅R1-01–03/R2-01–03/BUILD-R/DOC-R，无旧13/27/Context/store/全仓/长期tests。有效设计issue3-design及correction（更正优先，不自动20页回扫、不宽泛拒绝码）。代码src/cli/tui/client.go/model.go/panels.go，新residual_test.go；runtime报告reports/implementation/issue3正落盘最后跟hash。修前实际FAIL保留before.*与before-items-cursor.*，不要改写。
请读实现+新9parent29table场景测试，亲跑 go test -race ./cli/tui -run '^TestIssue2Residual' -count=1 -timeout=30s 与go vet ./cli/tui。核对两个状态机：确POST受理后任何GET错都保request/knownturn/meta无草稿无POST；仅真实precommit拒绝/POSTunknown对照；重建receipt->turn身份在后GET403时仍已accepted；终态不倒退不重复。页缓存range/epoch/gen/selectedID，延迟refresh/append/切panel防覆写，确认原ID/rev，items409后只selectedGET+404清选择/r原ID回绑，通知无额外写/ack（既有DELIVERED明示）。按真实HTTP契约避免假调用器过宽；有必要独立probe用/tmp，不改生产。报告实际mustfix或限定PASS、命令和完整关键hash，未测限制如实。不要重跑原TUI suite或backend，受影响CLI构建可BUILD-R、不重建daemon。最多14工具轮留3自然final，stdout归档；无keys/产品真实model/其他服务。
