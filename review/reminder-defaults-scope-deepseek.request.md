Ayanami，请不使用工具直接确认v2最小边界两点：
1 具体scope仅自然语言Decision CREATE_JOB且capability属于notify.local或alarm.play（二者均提醒）；其它非提醒job不加5分钟限制，认证Typed不变。自然语言明确要求自定义迟到策略时系统只能说明需显式Typed配置，不生成替代默认动作；guard只拒模型越出v1能力，不声称证明任意NL语义。
2 协调方未提出新增重试数值。v2 K2自行加入“总计<=2模型调用”不得覆盖既有Core固定有限重试/预算契约。请将K2更正为“沿用既有已文档化的Core语义校验重试上限与持久预算，不新增/放宽/重置次数；耗尽无副作用失败，反馈留档”。不为本缺陷单独新设常量。若你认为必须改变既有上限，请给具体必要性而不是凭空加验收门槛。
其余v2通道/拒绝/4测试/不改runtime-oracle不变。请简短同意或指出实质矛盾；仍DeepSeek v4.1 Flash。
