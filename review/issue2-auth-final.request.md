Master授权Issue2 AUTH01-08后端冻结模块实际独立终审，仅本轮AUTH/TUI重叠/DOC/BUILD，不旧Context/store/全仓/月回放/长期门槛。有效设计为issue2-auth-design及correction（更正优先，Typed_BUSY无新错误字段/explicitsession严格hash/旧tests不改不跑）。报告reports/implementation/issue2-auth.md/.json给8项map/sourcehash，请实际读映射实现与本轮tests，亲跑 go test -race ./store ./tests -run '^TestIssue2AUTH' -count=1（仅这个selector），可按BUILD跑go vet ./store ./core ./context ./memory ./transport；不要整包旧tests。
范围authority_repo+002 additive/版本helper、所有公共Store/Core/HTTP/Typed入口、单consumer/frozenSnapshot/READ_MEMORY/summarygap、TaskLocal（仅程序SYSTEM内部briefing只读terminal prefix，不创建/推进主会话，不能public绕过）；省略session旧replay/null显式foreign拒绝，history严格方向/分页；旧readonlypending不占queue，fakePERSONAL/SECRET零model，旧v1显式selection/两role锁/未知v3，少量reopen/sharedquestion冲突稳定回执。检查prefix是否排除future queued原文/分类/检索但保留前终结失败、后轮不会无限CAS重试；真实数据库是新迁移短样例不是旧恢复矩阵。用户要求所有新scope失败处理，不以作者PASS代审。必要独立probe在/tmp不改生产。不读key/真实资料/产品真实model，不动服务。输出限定PASS或具体mustfix、实际测试与源码hash；若TUI API尚对齐列明，不冒称TUI已验收。最多17工具轮留3自然final，stdout归档。
