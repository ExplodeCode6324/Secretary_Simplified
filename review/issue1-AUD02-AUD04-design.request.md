Ayanami，Master指定仅deepseek-v4.1-flash/opencode-go。Issue1的AUD02需最小结算协议修订，AUD04为读取限额修复；issue原文review/issue1-original.md（静态审计不等于实测）。只读相关两节及CoreWork/ExecutionProtocol/实现必要段，不重开全仓，不委派。Master明确不重跑两小时，也不新增任何替代持续时长门槛；不动正在跑soak、其data/binary，原证据保留原hash。此轮仅设计裁决，实施后独立定向/race/build/短HTTP e2e。
runtime提案：Core业务仍使用HTTP取消ctx；退出后5s WithTimeout(WithoutCancel(ctx))仅持久FinishWork，禁止继续模型/副作用。FinishWorkTx允许同run/task/attempt/fence/command身份且cancel_generation匹配对应durable permit的RUNNING或RESULT_UNKNOWN，只写CoreWork结果不直接变run；Runner GET Query取durable receipt再RecordReceipt控制状态。
取消Task同fence可保存迟到审计receipt但不复活；旧attempt/fence拒覆盖。memory.refresh查exactslot已提交则收尾SUCCEEDED；briefing已有本次可绑定ObjectRef则成功；模型取消且确定无业务写入FAILED/effectfalse按既有budget重试；source.sync已进入可能分批写失败UNKNOWN，不假无效果；world原子事务已有同txreceipt。Core进程崩溃无证据RUNNING仍可诊断UNKNOWN，不盲重发；BeginWork同代RUNNING拒重复Execute，避免同请求重复模型。
请明确必要状态/身份/取消验证与迟到审计记录边界；是否允许在任务取消/授权撤销后记录已经发生的结果而不执行新业务；保持取消Task不SUCCEEDED、未知不盲重试、旧代不可覆盖、新context只收尾5s非后台延长执行。证明exactslot/产物必须绑定本次目标/attempt合法，不拿任意旧结果算成功；无证据继续UNKNOWN不伪FAIL。支持AUD02列出的6个真实HTTP假模型取消/先UNKNOWN/产物已落场景，无新DTO/DDL是否可行，最小docs修改路径。
AUD04方向：open file后LimitReader(max+1)，超1MiB拒INPUT_TOO_LARGE，stat仅快速拒绝、实际读限必须保留；正确close，非regular拒避免无限阻塞（注意打开FIFO可能先阻塞，方案要解释打开阶段）；拒绝/读取失败不得推进source cursor/成功receipt。以小文件/边界/+1/读时增长可注入短测验证，不读真实文件。
输出可实施设计同意/条件/确切反对理由，保留静态与实测区分。不改业务或同名report。最多有限工具，预留自然总结。无真实模型产品调用/密钥/ELIZA/其他进程。真实Go429即停告root并协调官方，不单方换reviewer。
