diff --git a/docs/Operations.md b/docs/Operations.md
index 5ac88fe..5b8960f 100644
--- a/docs/Operations.md
+++ b/docs/Operations.md
@@ -69,3 +69,5 @@ P2 的本地播放不依赖模型或 Core，但依赖供电、操作系统、登
 
-断线或等待超时用原 request_id 查询受理回执及 turn，不自动创建新请求。客户端根据 instance_id 区分本地视图缓存；分页事件按稳定 ID 去重。任务控制与通知确认必须经认证接口，Runner 控制在 Core 离线时仍独立可用。
+断线或等待超时用原 request_id 查询受理回执及 turn，不自动创建新请求。已经收到 POST 受理确认后，后续查询失败不能推翻受理；恢复标识保留，界面提示查询不可用，401/403 需恢复认证。只有提交阶段确定未受理才可恢复空草稿，不能覆盖另一份新草稿。客户端重建从原 request 取得 turn；不额外保存私人正文。客户端根据 instance_id 区分本地视图缓存；分页事件按稳定 ID 去重。任务控制与通知确认必须经认证接口，Runner 控制在 Core 离线时仍独立可用。
+
+业务面板按稳定 ID 保持当前选择和已加载页。items 游标失效时保留画面并提示过期，暂停 n 翻页，只查询当前对象；使用 r 明确重载第一页，不静默跳页。目标消失或不在重载范围时提示、取消选择；再次操作需重新选择，旧确认仍按原目标/版本交后端验证。此 Issue #3 局部修复无需服务端迁移或重建 daemon/DB；只更新 CLI。恢复文件、凭据和数据分类边界不变。
 
