diff --git a/docs/ImplementationHandoff.md b/docs/ImplementationHandoff.md
index 9806bc3..de830c4 100644
--- a/docs/ImplementationHandoff.md
+++ b/docs/ImplementationHandoff.md
@@ -1,12 +1,10 @@
-# 当前实施交接：Issue #2
+# 当前实施交接：Issue #3
 
-项目已有 Go 源码、发布二进制、样例数据库及历史验收。当前工作是单一权威会话与非阻塞 TUI，不再从 M1 重做整个项目。
+项目已交付单一权威会话和非阻塞 TUI；当前只修复受理后查询误报拒绝与面板分页被刷新重置，不重做原架构或原验收。
 
-1. 阅读根 README、SingleConversationTUI、Interfaces、Storage 与当前 Issue #2。核对数据目录、唯一身份与迁移状态，禁止自动迁移或合并旧会话。
-2. AUTH 实现持久绑定、受理排序、冻结上下文前缀、共享认知状态、查询和幂等恢复；TUI 实现异步交互、结构化回答、控制确认及非 TTY 兼容。
-3. 每个模块由本地 Ayanami 使用 DeepSeek 复核。实施中发现的设计缺陷先商议，再同步实际受影响的正文、Schema/迁移镜像、示例与根 README 路径索引。
-4. 仅运行本轮 AUTH-01–08、TUI-01–13、DOC-01–05、BUILD-01 的短定向测试、必要 race、变更包 vet 和构建。不得把旧全仓/长时/模型测试变成本轮门槛；保留旧证据原字节并注明原构建归属。
-5. 文档逐文件影响清单、实现与证据映射存入 `reports/implementation/issue2/`。更新当前文档导出，扫描公开交付文件中的密钥和私人状态，发布后核对 GitHub 同步。
-6. 完成后通知 Master 接入真实文本测试。在独立目录中明确 PERSONAL 披露对象与范围；不得把真实资料送入固定 SYNTHETIC fixture。真实来源同步等能力另按已实现范围说明。
-
-后台任务由 Core/Runner 管理；客户端退出仅断开显示。超时后沿用原 request_id 查询，不能用新 ID 推断原操作未发生。历史多会话仅可只读查看，不通过 TUI 恢复为可写分支。
+1. 阅读 GitHub #3、SingleConversationTUI、Interfaces、Operations 与当前 reports/implementation/issue3 索引。c84d924 为原生产基线，原审计仅静态发现；修前实际测试结果单独保留。
+2. RES-01 分离提交与观察阶段，保持稳定 request/已知 turn、恢复元数据及终态；RES-02 保持页缓存、选中 ID、响应代次与确认目标，按既有 items 快照游标处理过期。
+3. 每个模块交本地 Ayanami DeepSeek 复核；局部设计修改按实际商议同步正文。公共 API、数据库、Schema、恢复文件 instance_id/request_id 与依赖不变，勿人为新增迁移。
+4. 只执行 TestIssue2Residual 定向 race、改动包 vet 和受影响 CLI 构建。不要运行旧 Context/Store/摘要/记忆/恢复、原 13/27 项、全仓、月回放、付费模型或 A25。daemon 和样例 DB 未变化则保持原字节。
+5. python3 scripts/issue3_docs_check.py --export 生成新的 v1.1.1 文档副本和本单逐文件影响清单；不覆盖 #2 的原清单/zip/测试报告。公开扫描也必须指定 issue3 新报告路径。
+6. 冻结实际修复结果、复核与构建哈希，提交 GitHub 并在 #3 回贴新证据。完成后通知 Master；真实文字测试仍在独立目录和明确披露范围内进行。
