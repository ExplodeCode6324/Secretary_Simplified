# Secretary 实施计划 v1.0

日期：2026-09-18。状态：设计完成待 Master 复核；新版应用未构建。根据 [原计划](archive/PLAN-2026-09-17.md) 和 [评审](InitialReview.md) 补全。当前设计入口为 [docs/README](README.md)。

本计划的通过条件由实际证据决定；文档齐全或上游探针通过不会自动勾选程序阶段。源码接入结论见 [PiIntegration](PiIntegration.md)，逐文件实现见 [ImplementationMap](ImplementationMap.md)。

## P0：基线与可验收范围

- [x] 明确单主会话、单子任务、文本初版及权威/信任边界。
- [x] 给出完整工程默认、数据契约、事务、恢复、记忆和验收设计。
- [x] 将初次评审 R1—R8 对应到具体设计与验收。
- [ ] Master 复核使用政策与范围；必要调整记录为决策修订。

交付：本套 docs、机器契约与独立验收清单。通过条件：目标/范围/不变量可明确判断；与未见的 9 月 16 日原文之间没有未经声明的继承假设。

## P1：工程骨架与 Pi 接入

- [x] 拉取完整 Pi 源码，核对发布 gitHead、npm integrity 和 Node 版本；保留版本差异发现。
- [x] 编写并运行上游接入探针与编译蓝图；结果见 research。
- [ ] 创建根工程、运行依赖锁、fixture 模型、结构校验和构建/测试入口。
- [ ] 实现 Secretary runtime/journal/model gateway，验证失败持久化、最终载荷门禁、取消和工具复验。
- [ ] 验证 OS 锁和最小沙箱/文件 broker：隔离失败拒绝、参数/路径逃逸拒绝。
- [ ] 冻结真实 provider profile 的 token 计数方法、序列化/重试/超时策略；此阶段可先仅有 fixture。

通过条件：应用自己的接入实验能证明阻断时零实际调用；运行历史无第二份权威；完整技术版本可重建。上游研究探针仅是部分前置证据。

## P2：持久化、状态机与恢复

- [ ] 实现 SQLite/ObjectStore、请求/事件/任务/授权/尝试/操作/回执/回复/outbox。
- [ ] 实现根预算、epoch/版本/取消代际、控制优先级和幂等冲突。
- [ ] 完成任务修改、固定验收条件、未知副作用核查以及无模型控制面。
- [ ] 实现证据对象完整性、备份恢复、迁移和删除基本契约。
- [ ] 使用 fixture 模型、虚拟时钟和进程故障注入跑通全部本阶段机制场景。

通过条件：所有纳入的机制用例通过；接收不丢、关键事务原子、重复不多执行、恢复可解释；查询/取消在模型挂起时仍可工作。

## P3：真实主子闭环

- [ ] 用合成文件完成真实模型的读取→证据→验收→正式回复→重启查询。
- [ ] 在主/子模型运行时继续输入、修改目标、取消、撤权，验证旧输出被正确处理。
- [ ] 隔离与未知副作用测试通过后开放专用目录内写入/命令。
- [ ] 运行第一轮真实模型留出评测，冻结指标、成本和配置；没有真实模型授权时保持 LIVE_MODEL_PENDING，不阻塞离线机制开发。

通过条件：真实只读闭环完成、受控操作符合范围、端到端证据可追踪；无越权/已接受请求丢失/可避免重复副作用。

## P4：事实记忆与回查

- [ ] 事实提案、自动接纳/待决规则、来源独立性、实体/时间语义与版本纠正。
- [ ] 任务/实体/时间/关键词检索，原文证据回读及有界分页。
- [ ] 纠正后的索引/摘要失效与在途提案版本检查。

通过条件：T06/T07/T24 等通过；当前事实与历史断言区分，查不到和检索失败区分。

## P5：增量工作记忆

- [ ] 独立义务记录、增量闭合事件整理、覆盖与连续水位。
- [ ] 原子发布工作记忆，积压/坏事件/重复处理恢复。
- [ ] 一致 Context 组装、必要未覆盖事件补入、最终请求追踪。
- [ ] 用独立 oracle 检测自然语言要求遗漏，不把结构合法等同于语义完整。

通过条件：已知关键要求无静默遗漏，模型整理失败不推进水位，旧检索材料不会成为新证据。

## P6：历史退出与长期边界

- [ ] 安全边界退出、工作集衰减与按需回查；轮换 Pi 对象释放内存。
- [ ] 超大输出/必要信息超预算/工具链未结束均有可恢复结果。
- [ ] 多次退出回放、长期请求/存储/成本测试、留出集与回归。

通过条件：退出前后任务、要求、事实一致；持续增长可测量且有容量/保留政策；无法满足预算时可停止与人工恢复。

## P7：扩展与受控真实使用

分别交付，不能一次勾选整个阶段：

- [ ] 持久定时/条件唤醒：实例唯一键、重启 catch-up、时区/DST、撤销和过期策略。
- [ ] 多子任务：资源冲突、公平配额、乱序回执和恢复。
- [ ] 多客户端：同一逻辑会话、认证/投递去重、主状态有序。
- [ ] 语音/视觉/安卓等输入：对象分类、转写不确定性、设备权限与专用验收。
- [ ] 7 天受控真实使用：Master 明确资料与外发范围，保留停止/备份/删除入口；真实失败进入回归。

语义检索和复杂遗忘评分只在对照实验有收益后引入。日程唤醒尚未实现时，不能向 Master 承诺会自动提醒。

## 每阶段提交规范

每个实现批次附：涉及的设计/契约 ID、目标文件、测试命令、fixture/版本、实际证据、未覆盖边界与回滚方式。应用实现与文档冲突时先修正决策及所有依赖附件，禁止只在代码里悄悄改变含义。
