# 当前实施交接：Issue #2

项目已有 Go 源码、发布二进制、样例数据库及历史验收。当前工作是单一权威会话与非阻塞 TUI，不再从 M1 重做整个项目。

1. 阅读根 README、SingleConversationTUI、Interfaces、Storage 与当前 Issue #2。核对数据目录、唯一身份与迁移状态，禁止自动迁移或合并旧会话。
2. AUTH 实现持久绑定、受理排序、冻结上下文前缀、共享认知状态、查询和幂等恢复；TUI 实现异步交互、结构化回答、控制确认及非 TTY 兼容。
3. 每个模块由本地 Ayanami 使用 DeepSeek 复核。实施中发现的设计缺陷先商议，再同步实际受影响的正文、Schema/迁移镜像、示例与根 README 路径索引。
4. 仅运行本轮 AUTH-01–08、TUI-01–13、DOC-01–05、BUILD-01 的短定向测试、必要 race、变更包 vet 和构建。不得把旧全仓/长时/模型测试变成本轮门槛；保留旧证据原字节并注明原构建归属。
5. 文档逐文件影响清单、实现与证据映射存入 `reports/implementation/issue2/`。更新当前文档导出，扫描公开交付文件中的密钥和私人状态，发布后核对 GitHub 同步。
6. 完成后通知 Master 接入真实文本测试。在独立目录中明确 PERSONAL 披露对象与范围；不得把真实资料送入固定 SYNTHETIC fixture。真实来源同步等能力另按已实现范围说明。

后台任务由 Core/Runner 管理；客户端退出仅断开显示。超时后沿用原 request_id 查询，不能用新 ID 推断原操作未发生。历史多会话仅可只读查看，不通过 TUI 恢复为可写分支。
